server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_mall?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

mall:
  ai:
    provider: deepseek
  storage:
    provider: local